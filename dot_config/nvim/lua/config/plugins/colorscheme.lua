return {
  {
    "navarasu/onedark.nvim",
    priority = 999,
    config = function()
      -- Theme setup is now handled in autocmds.lua for automatic system theme detection
      -- The initial configuration will be set there based on system appearance
    end,
  },
}
